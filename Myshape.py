class Myshape(object):
    def __init__(self, pos, velo, v_dir, acc, a_dir, shape, r, color, width, ifbound):
        self.position = pos
        self.velocity = velo
        self.direction=v_dir
        self.acc=acc
        self.A_dir=a_dir
        self.SHAPE=shape
        self.radius=r
        self.color=color
        self.width=width
        self.ifbound=ifbound

        

    
